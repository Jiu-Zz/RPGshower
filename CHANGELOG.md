# Changelog

RPGshower 的用户可见功能、数据行为和重要工程变化都记录在这里。版本遵循 SemVer 预发布规则：`MAJOR.MINOR.PATCH`，预发布版本使用 `-alpha.N`、`-beta.N` 或 `-rc.N`。

注意：应用版本和角色 JSON 的 `schemaVersion` 是两套独立版本。应用版本描述网页功能；角色 JSON 版本描述导入导出的数据格式。本项目当前应用版本为 `0.2.0-alpha.1`，角色 JSON 格式仍为 `0.1.0`。

## [Unreleased]

### Planned

- 完成 `0.2.0` 本地角色资料库的专用表单、搜索筛选和批量操作。

### Added

- `0.2.0` 首个迭代增加浏览器本地角色资料库。
- 支持新建、复制、切换、归档、恢复和删除角色。
- 角色资料库自动保存到浏览器 `localStorage`，刷新页面后保留角色。
- 增加本地资料库存储层的往返和异常数据测试。

## [0.2.0-alpha.1] - 2026-09-15

`0.2.0` v1 的首个开发预览版本，开始将单角色工作台升级为本地角色资料库。

### Added

- 浏览器 `localStorage` 资料库存储层，刷新页面后保留角色。
- 当前角色选择器，以及新建、复制、归档、恢复和删除操作。
- 新建角色使用安全的空白档案默认值；复制角色生成独立标识。

### Verification

- 18 项自动测试、Oxlint 和 Vite 生产构建通过。
- 本地 Edge 页面冒烟检查通过，资料库工具栏和角色卡正常渲染。

## [0.1.0] - 2026-09-15

v0 单角色工作台稳定版。发布候选阶段的功能、测试和浏览器回归均已完成验收。

### Verification

- 导入或加载角色、编辑、角色卡/档案预览、导出和再次导入的完整闭环已确认。
- TypeScript 检查、Oxlint、15 项自动测试和 Vite 生产构建全部通过。
- 桌面端 `1280x900` 与移动端 `375x812` 浏览器回归通过。

## [0.1.0-rc.1] - 2026-09-14

这是 v0 的发布候选版本，功能范围冻结，等待稳定版发布检查点。

### Verification

- 桌面端 `1280x900` 浏览器回归通过，页面有完整角色卡和编辑区，无横向溢出或控制台错误。
- 使用空的资源、属性、装备、技能及档案数组验证空状态，档案区域显示“暂无内容”。
- 使用长名称、标签、简介和档案文本验证换行，桌面端无横向溢出。
- 移动端 `375x812` 回归通过，文档宽度与视口一致，未发现元素横向溢出或区域重叠。
- JSON 编辑器成功应用上述边界数据，角色卡与档案视图均正确更新。

## [0.1.0-beta.1] - 2026-09-07

这是 v0 单角色工作台的 beta 版本，当前工作区版本。以下内容根据 Git 提交和本地已完成改动回溯整理，后续发布时应以验收结果为准。

### Added

- 增加 `archive` 档案区，支持背景、外貌、性格、目标、人物关系、经历和备注。
- 增加 `extensions` 扩展区，用于保留任意 JSON 自定义数据。
- 增加“角色卡 / 档案”视图切换和档案只读展示。
- 增加完整 JSON 编辑器，支持同步当前角色、应用 JSON 和未同步状态提示。
- 增加独立的角色 JSON 解析、序列化、下载和字段更新模块。
- 增加 15 项数据、兼容性、错误恢复和资源约束测试。
- 增加 v0 阶段目标和长期产品路线图文档。

### Changed

- `schemaVersion` 现在只接受角色 JSON 版本 `0.1.0`。
- 旧的 `0.1.0` 角色文件缺少 `archive` 或 `extensions` 时会自动补默认值。
- 资源值统一要求非负，且当前值不能超过最大值；基础表单会自动截断，JSON 会拒绝无效数据。
- JSON 错误区分语法错误和 Schema 错误，并显示全部字段路径。
- `App.tsx` 的角色编辑、导入导出和校验职责已拆出到独立模块和组件。
- README 从 Vite 模板说明改为项目使用说明和数据格式说明。

### Verification

- TypeScript 检查通过。
- Oxlint 通过。
- 15/15 自动测试通过。
- Vite 生产构建通过。
- 本地开发页面可访问，角色卡和档案视图已在浏览器中验证。

## [0.1.0-beta.0] - 2026-07-11

### Added

- 增加 React state 保存当前角色。
- 增加姓名、称号、简介、基础资料、资源值和属性值的基础表单编辑。
- 增加 JSON 文件导入、导出和 Zod 运行时校验。
- 将表单拆分为角色资料表单和基础资料表单组件。

## [0.1.0-alpha.1] - 2026-07-10

### Added

- 创建 Vite + React + TypeScript 应用。
- 建立 RPGshower 工作台外壳和响应式双栏布局。
- 创建由示例数据驱动的静态角色卡。
- 增加角色数据类型和角色卡、头部、资源、属性、装备、技能组件。
